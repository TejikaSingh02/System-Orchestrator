services:
  - type: web
    name: system-orchestrator
    env: node
    buildCommand: npm install
    startCommand: node src/server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
